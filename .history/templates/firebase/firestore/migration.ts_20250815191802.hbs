// Migration Firestore v{{migration.version}}
// {{migration.description}}
// Date: {{timestamp}}

import { db } from '@/lib/firebase';
import { doc, setDoc, deleteDoc, collection, getDocs } from 'firebase/firestore';

export async function up() {
  try {
    console.log(`🚀 Exécution de la migration v{{migration.version}}: {{migration.description}}`);
    
    // Code de migration vers l'avant
    {{migration.up}}
    
    console.log(`✅ Migration v{{migration.version}} exécutée avec succès`);
  } catch (error) {
    console.error(`❌ Erreur lors de la migration v{{migration.version}}:`, error);
    throw error;
  }
}

export async function down() {
  try {
    console.log(`🔄 Annulation de la migration v{{migration.version}}: {{migration.description}}`);
    
    // Code de migration vers l'arrière
    {{migration.down}}
    
    console.log(`✅ Migration v{{migration.version}} annulée avec succès`);
  } catch (error) {
    console.error(`❌ Erreur lors de l'annulation de la migration v{{migration.version}}:`, error);
    throw error;
  }
} 